// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "org.nativescript.umber"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
