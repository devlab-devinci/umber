// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "org.umber.application"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
